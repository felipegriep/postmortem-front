// file intentionally removed: auth-login.js stub removed in favor of Angular's AuthLoginService
// The app now relies on AuthLoginService (exposed via APP_INITIALIZER) to handle /auth/login.
// Kept this file as a placeholder to avoid missing asset errors during deploy; it's intentionally empty.

